# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

For local async key-value storage, use `expo-sqlite/kv-store`. Do not install `@react-native-async-storage/async-storage`, and do not use `expo-sqlite/localStorage/install`.
